import { ImageResizerDirective } from './image-resizer.directive';

describe('ImageResizerDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageResizerDirective(null);
    expect(directive).toBeTruthy();
  });
});
