import { MembersDirective } from './members.directive';

describe('MembersDirective', () => {
  it('should create an instance', () => {
    const directive = new MembersDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
