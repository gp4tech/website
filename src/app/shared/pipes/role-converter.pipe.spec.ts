import { RoleConverterPipe } from './role-converter.pipe';

describe('RoleConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new RoleConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
