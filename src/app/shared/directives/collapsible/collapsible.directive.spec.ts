import { CollapsibleDirective } from './collapsible.directive';

describe('CollapsibleDirective', () => {
  it('should create an instance', () => {
    const directive = new CollapsibleDirective(null);
    expect(directive).toBeTruthy();
  });
});
