import { TransparentSectionDirective } from './transparent-section.directive';

describe('TransparentSectionDirective', () => {
  it('should create an instance', () => {
    const directive = new TransparentSectionDirective(null);
    expect(directive).toBeTruthy();
  });
});
