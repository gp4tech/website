import { CarouselDirective } from './carousel.directive';

describe('CarouselDirective', () => {
  it('should create an instance', () => {
    const directive = new CarouselDirective(null);
    expect(directive).toBeTruthy();
  });
});
